import { render, screen } from '@testing-library/react';
import Navigation from './Navigation';

describe('Navigation component ', () => {
  //   it('Navigation snapshot Auth', () => {
  //     const userMenu = render(<Navigation isAuthenticated={true} />);
  //     expect(userMenu).toMatchSnapshot();
  //   });
  //   it('Navigation snapshot  not Auth', () => {
  //     const userMenu = render(<Navigation isAuthenticated={false} />);
  //     expect(userMenu).toMatchSnapshot();
  //   });
});
